#include <stdint.h>
#include <stdio.h>
void init_sensor(uint16_t *variable);

void move_pap(uint16_t variable);

void move_pap_coordenate (uint16_t variable_x, uint16_t coordenate_x, uint16_t variable_y, uint16_t coordenate_y);
